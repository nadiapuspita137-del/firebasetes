{
  "rules": {
    ".read": false,
    ".write": false,
    "appConfig": {
      ".read": true,
      ".write": true,
      "rooms": {
        "$roomId": {
          ".validate": "newData.hasChildren(['name', 'codeHash', 'active'])",
          "name": { ".validate": "newData.isString() && newData.val().length >= 2 && newData.val().length <= 40" },
          "codeHash": { ".validate": "newData.isString() && newData.val().matches(/^[a-f0-9]{64}$/)" },
          "active": { ".validate": "newData.isBoolean()" }
        }
      },
      "adminCodeHash": {
        ".validate": "newData.isString() && newData.val().matches(/^[a-f0-9]{64}$/)"
      },
      "accounts": {
        "$accountId": {
          ".read": true,
          ".write": true,
          ".validate": "!newData.exists() || (newData.hasChildren(['accountId','username','usernameLower','roomId','passwordHash','salt','status']) && newData.child('accountId').val() === $accountId && newData.child('username').isString() && newData.child('username').val().length >= 3 && newData.child('username').val().length <= 24 && newData.child('passwordHash').val().matches(/^[a-f0-9]{64}$/) && newData.child('salt').val().matches(/^[a-f0-9]{32}$/) && (newData.child('status').val() === 'pending' || newData.child('status').val() === 'approved' || newData.child('status').val() === 'rejected'))"
        }
      }
    },
    "rooms": {
      "$roomId": {
        ".read": true,
        ".write": true,
        "messages": { ".indexOn": ["timestamp", "userId"] },
        "users": { ".indexOn": ["lastSeen"] },
        "presence": { ".indexOn": ["userId", "at"] }
      }
    }
  }
}
